-----------------------------------------------------
매 시각 안에 특정 숫자 확인

if '3' in str(hour)+str(min)+str(sec):
    cnt+=1

-----------------------------------------------------
아스키코드

ord('a')
a를 아스키코드 값으로변환
chr(97)
아스키코드를 a로 변환

48-'0' 65-'A' 97-'a'
-----------------------------------------------------
개행

print("abcd\ndef")
or
print("""abcd
def""")
------------------------------------------------------
문자열 덧셈,곱셈 가능 //O(n)
------------------------------------------------------
인덱싱

s[-0]=s[0]
s[-1]활용 잘하기
------------------------------------------------------
문자열 슬라이싱

덧셈으로 사용해서 붙여도되지만
s[0:4] 로 슬라이싱 //끝번호는 포함 x임

s[:] //처음부터 끝까지
s[4:-1] //4번인덱스부터 끝까지

ex)
s="20201203Thursday"
a=s[:8]
b=s[8:]
------------------------------------------------------
하나의 인덱스 변경

*문자열은 immutable하기 때문에 요소값 변경이 안된다. 다시 만들어주어야함
ex)
Pithon을 Python으로 변경
s="Pithon"
s=s[:1]+"y"+s[2:]
s=s.replace('i','y',1) //3번째 인자 1은 인덱스가 아니라 치환갯수임
------------------------------------------------------
문자열 포매팅

print("I eat %d apples"%3)
print("I eat %s apples"%"three")
print("I eat %d apples and %s melons" %(3,"three"))

%d, %c, %s, %f-부동소수, %o, %x-16진수, %%
------------------------------------------------------
------------------문자열관련 함수들----------------------
------------------------------------------------------
문자열 개수 세기

s="abbbab"
print(s.count('b'))
>>>4
print(s.count('bb'))
>>>1
-------------------------------------------------------
문자 위치 찾기

s="abbbab"

print(s.find("bb"))
>>1
print(s.find("bbb"))
>>1
print(s.find("a"))
>>0
print(s.find("a",3))
>>4
print(s.find("z"))
>>-1
s.index("z")는 오류 반환

앞에서부터 해당 문자열 찾는다가정할때 인덱스 비교하는것 상상해보면될듯
--------------------------------------------------------
문자열 삽입 join() 인자는 iterable해야함

",".join("abcd")
>>>'a,b,c,d'

",".join(['a','b','c','d'])
>>>'a,b,c,d'

li=['e','u','n']
"".join(li)
>>>eun
--------------------------------------------------------
소문자->대문자
s.upper()
대문자->소문자
s.lower()
왼쪽공백 지우기
s.lstrip()
오른쪽공백 지우기
s.rstrip()
양쪽공백 지우기
s.strip()
---------------------------------------------------------
문자열 변경 (replace함수)

s="aabbbaab"
b=s.replace("aa","cc")
print(s)
>>>aabbbaab
print(b)
>>>ccbbbccb
d=s.replace("aa","cc",1) //세번째 인자는 index가 아니라 치환 개수임
print(d)
>>>ccbbbaab
----------------------------------------------------------
문자열 나누기

s="Life is too short"
arr=s.split()
print(arr)
>>>['Life','is','too','short']

-----------------------------------------------------------
print시 끝에 개행대신 문자 추가해서 출력하기
print("hi",end='')
개행대신 아예 안넣어서 개행만 없애기가능, 다른 문자도 가능

-----------------------------------------------------------
input()과 다르게 readline하면 개행까지 읽어지므로 rstrip해주어야한다(문자열로 받을경우 len값이 달라질수있음)
stdin.readline().rstrip('\n')

